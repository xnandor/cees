# Ignore everything
*
# Don't ignore directories, so we can recurse into them
!*/
# Don't ignore .gitignore and *.foo files
!.gitignore
!docker-compose.yml
!./README.md
!*gogs.app.ini
!*nginx.conf
!*xwiki-entrypoint.sh
